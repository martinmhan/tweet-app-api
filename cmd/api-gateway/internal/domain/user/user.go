package user

// ValidateNewUsername checks if a user already exists with a given username
func ValidateNewUsername(username string) bool {
	return true
}

// ValidatePassword checks if the password is correct for the given username
func ValidatePassword(username string, password string) bool {
	return true
}
